/**
 * a core component to run components it is the base build block of a component-manager 
 * it is save to expose this as this implementation can only run what it gets passed
 * if you do not pass something to it then it also can not execute something.
 */
 export const run = (Tasks) => {} // Returns component-* instance created out of tasks. 
