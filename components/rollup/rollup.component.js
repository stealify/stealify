/**
 * Rollup Component definition
 * implements component-rollup use able via 
 * import { rollup } from 'component:rollup';
 * const { rollup } = await import('component:rollup');
 * rollup(Task, Task, Task, Task)
 */
